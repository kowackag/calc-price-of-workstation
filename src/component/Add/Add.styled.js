import styled from 'styled-components';

const StyledApp = styled.span`
    position: absolute;
    font-size: 2.5rem;
    
    & svg {
        position: relative;
        left: 10px;
        cursor: pointer;
    }
`

export default StyledApp;